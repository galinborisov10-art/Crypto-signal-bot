# Чувствителни данни
admin/credentials.json
admin/admin_password.json
*.env
.env

# Security (NEW - v2.0.0)
.encryption_key
.bot_token_encrypted
.bot_token_hash
.token_backups/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# Логове
*.log
bot.log
nohup.out

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Backup файлове
*.bak
*.swp
*.tmp
*.backup
*.backup_*
bot.py.backup*

# Stats (опционално - може да се вземе backup)
# bot_stats.json
# Добави .env файл в gitignore
.env

# Добави ML модел файлове в gitignore
ml_model.pkl
*.pkl

# ML и Stats файлове (не трябва да се commit-ват)
bot_stats.json
bot_stats.json.backup
backtest_results.json
daily_reports.json
trading_journal.json
active_auto_signals.json

# Backtest результати и архив (динамични данни)
backtest_results/
backtest_archive/
ict_backtest_results.json

# ML model файлове
ml_model.pkl
ml_scaler.pkl
*.pkl

# Database files (runtime data - should not be committed)
*.db
positions.db

# Cache files (NEW - Phase 2)
cache/
cache/*.json
!cache/.gitkeep
test_distance_filter_removal.py

# Test files for NO_ZONE logging validation (temporary)
test_no_zone_logging.py
test_fallback_logging_integration.py
demo_no_zone_logging.py
validate_no_zone_logging.py

# Audit data (production data - should not be committed)
audit_data/
sent_signals_cache.json

# TypeScript / Node.js
node_modules/
dist/
*.tsbuildinfo
coverage/
.npm
.eslintcache

# Test files for premium signal config
test_premium_signal_config.py
test_file_validation.py

# Temporary test files for logging fix
test_logging_fix.py
test_no_duplicate_logging.py

# Diagnostic reports (generated files)
diagnostic_report_*.md
