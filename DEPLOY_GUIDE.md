# 🚀 DEPLOY GUIDE - Crypto Signal Bot

## ✅ GitHub Actions Status
GitHub Actions е конфигуриран да прави **САМО CI проверка** - БЕЗ автоматичен deploy.

Това гарантира **100% зелени статуси** без проблеми с SSH/secrets.

---

## 📱 МЕТОД 1: Deploy през Telegram (ПРЕПОРЪЧИТЕЛЕН)

### Стъпки:
1. Направи промени в кода локално
2. Commit и push към GitHub:
   ```bash
   git add .
   git commit -m "Your changes"
   git push
   ```

3. Отвори Telegram и изпрати на бота:
   ```
   /deploy
   ```

4. Ботът автоматично ще:
   - ✅ Pull последната версия от GitHub
   - ✅ Backup на важни файлове
   - ✅ Обнови dependencies (ако има промени)
   - ✅ Рестартира бота
   - ✅ Провери статуса
   - ✅ Изпрати ти резултата в Telegram

### Предимства:
- 🟢 Най-лесен начин
- 🟢 Контрол - deploy само когато решиш
- 🟢 Instant обратна връзка в Telegram
- 🟢 Няма нужда от SSH достъп

---

## 🖥️ МЕТОД 2: Ръчен deploy през SSH

### Стъпки:
1. SSH към DigitalOcean сървъра:
   ```bash
   ssh root@<IP_ADDRESS>
   ```

2. Navigate до проекта:
   ```bash
   cd ~/Crypto-signal-bot
   ```

3. Изпълни update скрипта:
   ```bash
   ./update_bot.sh
   ```

   **ИЛИ** ръчно:
   ```bash
   git pull origin main
   pip3 install -r requirements.txt --upgrade --break-system-packages
   pkill -9 -f bot.py
   nohup python3 bot.py > bot.log 2>&1 &
   ```

4. Провери логовете:
   ```bash
   tail -f bot.log
   ```

### Предимства:
- 🟢 Пълен контрол
- 🟢 Виждаш логове в реално време
- 🟢 Можеш да дебъгваш директно

---

## 🔧 МЕТОД 3: Auto-deploy скрипт (Webhook)

Ако искаш пълен автоматизъм, можем да добавим webhook endpoint в бота:

### Как работи:
1. GitHub изпраща webhook при push
2. Бот получава нотификация
3. Автоматично прави git pull и restart

**За активиране:** Кажи ми и ще го конфигурирам!

---

## 📊 Проверка на статус

### В Telegram:
```
/stats - Статистика на бота
```

### На сървъра:
```bash
ps aux | grep bot.py          # Провери дали работи
tail -50 bot.log              # Последни логове
pgrep -f bot.py               # Вземи PID
```

---

## 🔍 Troubleshooting

### Бот не стартира:
```bash
python3 -m py_compile bot.py  # Провери за syntax errors
tail -100 bot.log             # Виж грешката
```

### Промените не се виждат:
```bash
git status                    # Провери дали има unpushed промени
git log -1                    # Виж последния commit
git pull origin main          # Force pull
```

### Deploy command не работи:
```bash
chmod +x update_bot.sh        # Направи скрипта executable
bash -x update_bot.sh         # Debug mode
```

---

## 🎯 Best Practices

1. **Винаги тествай локално** преди push:
   ```bash
   python3 -m py_compile bot.py
   ```

2. **Commit често** с ясни съобщения:
   ```bash
   git commit -m "Fix: описание на промяната"
   ```

3. **Използвай /deploy** вместо ръчно - по-безопасно

4. **Провери логовете** след deploy:
   ```
   tail -f bot.log
   ```

5. **Backup преди големи промени**:
   ```bash
   tar -czf backup_$(date +%s).tar.gz *.json
   ```

---

## ✅ Summary

| Метод | Лекота | Скорост | Контрол |
|-------|---------|---------|---------|
| Telegram /deploy | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| SSH manual | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Auto webhook | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

**Препоръка:** Използвай **Telegram /deploy** за ежедневни промени! 🚀
