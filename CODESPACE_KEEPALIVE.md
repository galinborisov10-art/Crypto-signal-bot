# 🚀 Codespace Keep-Alive System

## 📋 Какво прави:
Този system гарантира че Codespace остава активен и ботът работи непрекъснато.

## 🔧 Компоненти:

### 1. `keep_codespace_alive.sh`
- Работи на всеки 5 минути
- Прави git fetch операции
- Проверява статус на бота
- Записва логове в `/tmp/codespace_keepalive.log`

### 2. `startup.sh`
- Автоматично стартира бота при отваряне на Codespace
- Стартира keep-alive процеса
- Показва статус на системата

### 3. `.devcontainer/devcontainer.json`
- Конфигурира Codespace да изпълни `startup.sh` автоматично
- Настройва Python среда

## 📊 Как да проверя дали работи:

```bash
# Провери keep-alive процес
ps aux | grep keep_codespace_alive

# Виж логове
tail -f /tmp/codespace_keepalive.log

# Провери бота
ps aux | grep "python3 bot.py"
tail -f /tmp/bot.log
```

## 🔄 Ръчно стартиране:

```bash
# Стартирай всичко
./startup.sh

# Само keep-alive
./keep_codespace_alive.sh &

# Само бота
python3 bot.py &
```

## ⚠️ Важно - GitHub Codespaces лимити:

**Free tier:**
- **60 часа месечно**
- **Автоматично спира след 30 мин неактивност**
- **Максимум 4 часа continuous runtime**

**За повече време:**
1. GitHub Settings → Codespaces → Default idle timeout
2. Промени на 240 минути (4 часа)
3. Или upgrade на GitHub Pro ($4/месец) = 180 часа

## 📝 Логове:

- Bot: `/tmp/bot.log`
- Keep-Alive: `/tmp/codespace_keepalive.log`
