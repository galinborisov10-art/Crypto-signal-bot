name: 🚀 Deploy Notification

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
    - name: ✅ Build Success
      run: echo "Build completed successfully"

