name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Verify syntax
      run: |
        echo "✅ Repository updated successfully"
        echo "Use /deploy in Telegram to update DigitalOcean server"
