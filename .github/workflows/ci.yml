name: ✅ CI Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - name: ✅ Checkout Code
      uses: actions/checkout@v3
    
    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: ✅ Check Syntax
      run: |
        python -m py_compile bot.py
        echo "✅ Code syntax is valid"
    
    - name: 📦 Check Dependencies
      run: |
        pip install -r requirements.txt
        echo "✅ All dependencies can be installed"
    
    - name: ✅ Build Success
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ Code pushed successfully to GitHub!"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "📱 To deploy to DigitalOcean server:"
        echo "   1. Use Telegram: /deploy"
        echo "   2. OR SSH manually: cd ~/Crypto-signal-bot && git pull && ./update_bot.sh"
        echo ""
        echo "🤖 Bot will auto-update when you run /deploy in Telegram"
