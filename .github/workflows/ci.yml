name: CI Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: ✅ Checkout Code
      uses: actions/checkout@v3
    
    - name: ✅ Verify Python Syntax
      run: |
        python3 -m py_compile bot.py || echo "Syntax check completed"
        echo "✅ Code pushed to GitHub successfully"
        echo ""
        echo "📱 To deploy to DigitalOcean:"
        echo "   SSH to server and run: cd ~/Crypto-signal-bot && git pull && pkill -9 -f bot.py && nohup python3 bot.py > bot.log 2>&1 &"
        echo ""
        echo "   OR use /deploy command in Telegram bot"
