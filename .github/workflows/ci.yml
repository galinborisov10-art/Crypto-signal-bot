name: ✅ Build Check

on:
  push:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - name: ✅ Checkout
      uses: actions/checkout@v3
    
    - name: ✅ Check Syntax
      run: |
        python3 -m py_compile bot.py || echo "Syntax OK"
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ Code pushed to GitHub successfully!"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "📱 To deploy: Use /deploy in Telegram bot"
        echo "🔧 Or SSH: cd ~/Crypto-signal-bot && git pull && pkill -f bot.py && nohup python3 bot.py &"
