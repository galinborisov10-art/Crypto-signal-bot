name: ✅ CI Check Only

on:
  push:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - name: ✅ Checkout
      uses: actions/checkout@v3
    
    - name: ✅ Syntax Check
      run: |
        python3 -m py_compile bot.py
        echo "✅ Code is valid"
        echo ""
        echo "📱 Deploy with: /deploy in Telegram"
