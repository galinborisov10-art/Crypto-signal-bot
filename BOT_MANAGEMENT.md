# Crypto Signal Bot - Управление

## 🚀 Бърз старт

Ботът вече е настроен да работи **автоматично**!

### Команди за управление:

```bash
# Стартиране
./bot-manager.sh start

# Спиране
./bot-manager.sh stop

# Рестарт
./bot-manager.sh restart

# Статус
./bot-manager.sh status
```

## 🔄 Автоматично стартиране

Ботът ще стартира **автоматично** при:
- ✅ Отваряне на нов терминал
- ✅ Рестарт на Codespace
- ✅ След системен crash

**Как работи:**
- Добавен в `~/.bashrc` автоматичен старт
- При всяко отваряне на терминал проверява дали ботът работи
- Ако не работи - стартира го автоматично

## 📁 Файлове

- `bot-manager.sh` - Главен скрипт за управление
- `autostart.sh` - Autostart скрипт (извиква се от .bashrc)
- `bot.pid` - PID на работещия процес
- `bot.log` - Логове на бота

## 🛡️ Функции за надеждност

### Auto-restart
Ботът **няма** да спре дори ако:
- Затвориш терминала
- Прекъснеш връзката
- Logout/Login

### Мониторинг
```bash
# Виж статус
./bot-manager.sh status

# Виж последни логове
tail -f bot.log

# Виж само важни съобщения
tail -f bot.log | grep -E "(🚀|✅|🔔|ERROR|🤖)"
```

## 📊 Проверка

След рестарт на терминал, ботът трябва автоматично да се стартира.

Провери с:
```bash
./bot-manager.sh status
```

## ⚠️ Важно

- PID файлът (`bot.pid`) съхранява процес ID
- Логовете се записват в `bot.log`
- При грешка ботът се рестартира автоматично
- Интервал на auto-alerts: **5 минути**

---

**Готово!** Ботът вече работи **24/7** в background! 🚀
